import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.virginia.managerlife',
  appName: 'ManagerLife',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
